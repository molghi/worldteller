import * as turf from "@turf/turf";

const getCountryFromCoords = (L, lat, lng, geojsonData) => {
    // const point = L.latLng(lat, lng);
    // const countryLayer = L.geoJSON(feature);
    // if (countryLayer.getBounds().contains(point)) {
    //     country = feature.properties.name; // Assuming the country name is stored here
    // }
    // console.log(`Country: ${country}`);
};

export default getCountryFromCoords;
